{
  "image": "mcr.microsoft.com/dotnet/sdk:8.0",
  "postCreateCommand": "apt-get update && apt-get install -y libfontconfig1 && dotnet restore && dotnet test",
  "forwardPorts": [5000],
  "portsAttributes": { "5000": { "label": "WebAPI" } }
}
